# AGENTS.md — בינה לתעשייה

הנחיות לסוכני בינה מלאכותית ולבני צוות העובדים על הריפו הזה. יש לקרוא ולפעול לפי הקובץ הזה כשהוא רלוונטי לעבודה.

## כללי עבודה (חלים על כל סוכן ובן צוות)

- כל סוכן פותח ענף אחד בלבד לעבודתו. אין לפתוח ענפים נוספים ללא בקשה מפורשת.
- אסור למזג לענף הראשי (פרודקשן). מיזוג לפרודקשן נעשה אך ורק על ידי בעל הריפו, לאחר אישור מפורש.
- דבר תמיד בעברית עם המשתמש. כל התשובות, ההסברים, וההערות לקוד — בעברית, אלא אם התבקשה במפורש שפה אחרת.
- כשכותבים עברית (RTL), אין לשלב מילים באנגלית, פקודות, נתיבים או מזהים באמצע משפט. יש להציב כל מונח לועזי, פקודה, נתיב, מזהה או מספר `PR`/`commit` בשורה נפרדת ובתוך גרשיים אחוריים (backticks), כדי למנוע ערבוב כיווני טקסט.

## הגדרת השירותים והמיצוב העסקי (קוהרנטיות מלאה)

1. **אפיון וכתיבת נהלי עבודה (SOPs) לכל רוחב העסק**:
   מיפוי ועיגון הידע הארגוני לכל רוחב המחלקות — בדגש על שימור ידע, המשכיות עסקית, סקיילביליות (Scalability) ויצירת ערך ממשי ללא תלות בעובד יחיד (לא להתמקד באופן צר בהצעות מחיר).
2. **סדנאות Hands-on והכשרת צוותים**:
   תרגול ישיר על משימות אמת, הנדסת פרומפטים מתקדמת ובניית סוכני AI ממוקדים לצוותים.
3. **אבחון תהליכים וצווארי בקבוק**:
   מיפוי משימות גוזלות זמן וחישוב ROI מוחשי ומדיד.
4. **פיתוח סוכני AI ואוטומציות קצה-לקצה**:
   סוכנים מותאמים אישית (פרויקט BinaTor מהווה Case Study והוכחת יכולת טכנולוגית בלייב).
5. **ערוץ פנייה יחיד באתר**:
   וואטסאפ 053-6244330 בלבד.

## מבנה הפרויקט

אתר שיווקי בעברית (RTL) עם קליטת פניות ואוטומציה, בלי `Zapier`. הפרונטאנד:
- `React`
- `TypeScript`
- `Vite`
- `Tailwind CSS`

הבקאנד רץ כפונקציות `Netlify`:
- `netlify/functions/chat.ts` → `/api/chat` (צ'אט דרך `OpenRouter`)
- `netlify/functions/lead.ts` → `/api/lead` (קליטת פנייה, ניתוח, והתראות)

לוגיקה משותפת נמצאת תחת:
- `shared/`

בזמן פיתוח מקומי, פלאגינים של `Vite` תחת `vite-plugins/` מדמים את שתי הפונקציות האלו, כך שאין צורך ב-`netlify dev`.

## פקודות עיקריות

מנהל החבילות היחיד בפרויקט הוא `npm` (קובץ הנעילה: `package-lock.json`). אין להשתמש ב-`pnpm` או ב-`yarn` — כדי לשמור על עקביות מול `Netlify` ומול הסביבה.

- התקנת תלויות:
  `npm ci`
- הרצת שרת פיתוח (אתר + `/api/chat` + `/api/lead`) על פורט `5173`:
  `npm run dev`
- בנייה לפרודקשן (כולל בדיקת טיפוסים):
  `npm run build`
- תצוגה מקדימה של הבנייה:
  `npm run preview`

אין בריפו סקריפט `lint` או `test` ייעודי. בדיקת הטיפוסים מתבצעת כחלק מ-`npm run build` דרך:
`tsc --noEmit`

## סביבת סוכני ענן

הסביבה מוגדרת בקובץ:
`.cursor/environment.json`

היא משתמשת בתמונת הבסיס הדיפולטית (`Node 22`), מתקינה תלויות עם `npm ci`, ומריצה את שרת הפיתוח של `Vite` כטרמינל רקע דרך:
`npm run dev -- --host`

גרסת ה-`Node` מוצמדת ל-`22` בקובץ `.nvmrc`, כדי שפיתוח מקומי, סוכני ענן, ובניית פרודקשן ב-`Netlify` ירוצו על אותה גרסה.

סודות שמוזרקים כמשתני סביבה (כולל דרך מסך ה-`Secrets` של סוכן הענן) נקראים אוטומטית על ידי שרת הפיתוח — אין צורך בקובץ `.env` בסביבת הענן.

## משתני סביבה וסודות

כל הסודות אופציונליים לפיתוח מקומי — האפליקציה עובדת מקצה לקצה בלעדיהם ומדלגת בחן על ערוצים לא מוגדרים. הפירוט המלא נמצא ב-`README.md` וב-`.env.example`. המשתנים העיקריים:
- `OPENROUTER_API_KEY` — צ'אט וניתוח פניות
- `TELEGRAM_BOT_TOKEN` — התראות טלגרם
- `RESEND_API_KEY` — התראות מייל
- `GOOGLE_SHEETS_WEBHOOK_URL` — כתיבה לגיליון

בפיתוח מקומי מגדירים אותם בקובץ:
`.env.local`

בסוכני ענן מגדירים אותם דרך מסך ה-`Secrets`. אין לקודד מפתחות בקוד או ב-`git`.

**חשוב:** אין לשדרג או להפעיל מחדש את ה-`Zap` הישן — `/api/lead` מחליף אותו לחלוטין.
